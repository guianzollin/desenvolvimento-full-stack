## Setup

- Instalar dependências: composer install
- Criar arquivo .env (a partir de .env.example)
- Criar chave da aplicação: php artisan key:generate
- Configurar variáveis de conexão com o banco de dados
- Inicializar servidor: php artisan serve
